<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#8B0000"
    android:padding="20dp"
    tools:context=".UpgradeDatabase">

    <TextView
        android:id="@+id/tvassign"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ASSIGN NEW EXCOS HERE"
        android:textStyle="bold"
        android:layout_centerHorizontal="true"
        android:textColor="@color/White"
        android:textAlignment="center"
        android:textSize="30sp"
        />

    <Spinner
        android:id="@+id/spinnnerposts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvassign"
        android:layout_marginTop="58dp"
        android:background="@drawable/edittext_bg"
        android:spinnerMode="dropdown" />

    <Button
        android:id="@+id/btnselectmember"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/spinnnerposts"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp"
        android:background="@drawable/button_bg_mod"
        android:text="Select Member" />

</RelativeLayout>